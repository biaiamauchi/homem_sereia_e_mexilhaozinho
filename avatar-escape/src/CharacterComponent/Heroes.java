package CharacterComponent;

import javafx.scene.image.ImageView;

public class Heroes extends Character{
    public Heroes(String character, String imageSource, int line, int collumn, int score, int life){
        super(character, imageSource, line, collumn, score, life);
    }
}
