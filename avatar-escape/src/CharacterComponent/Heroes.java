package CharacterComponent;

import javafx.scene.image.ImageView;

public class Heroes extends Character{
    public Heroes(String character, int line, int collumn, int score, int life){
        super(character, line, collumn, score, life);
    }
}
